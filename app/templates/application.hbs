{{transactions-table
    transactions =  model.transactions
    select       =  "selectTransaction"
}}

{{#if model.selectedNode}}
    {{flame-ui
        node    = model.selectedNode
        op      = model.selectedOp
        data    = model.flames
        select  = "selectSpan"
    }}

    {{#if model.selectedSpan}}
        {{span-log
            span =  model.selectedSpan
        }}
    {{/if}}
{{/if}}
